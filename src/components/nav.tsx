import { NavContent } from "./nav-content";

// TODO: Reconnect card count check via Payload API
export const Nav = () => {
    return (
        <nav>
            <NavContent />
        </nav>
    );
};
